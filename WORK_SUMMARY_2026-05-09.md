# Wiki 重新分類工作總結 - 2026-05-09

## 🎯 目標
使用成功的地理編碼結果（723 個）來重新分類 `wiki/未知` 中的檔案。

## ✅ 完成工作

### 1. 英文/中文資料夾整合
**時間**: 約 5 分鐘
**內容**: 合併所有英文國家資料夾到中文名稱
**結果**:
- 共 17 個國家資料夾被整合
- 248 個檔案從英文資料夾移動到中文資料夾
- 所有英文資料夾已刪除
- wiki 目錄現在只有中文名稱的國家資料夾

**整合的國家**:
```
China → 中國
Taiwan → 台灣
Japan → 日本
Thailand → 泰國
Vietnam → 越南
Cambodia → 柬埔寨
Malaysia → 馬來西亞
Singapore → 新加坡
United States → 美國
Canada → 加拿大
Hong Kong → 香港
Macao → 澳門
South Korea → 韓國
India → 印度
Egypt → 埃及
Netherlands → 荷蘭
New Zealand → 紐西蘭
```

### 2. CSV 檔案分離
**時間**: 約 2 分鐘
**內容**: 從 `batch2_success.csv` 分離成功和失敗的項目

**生成的檔案**:
- ✅ `batch2_success.csv` - 723 個成功編碼的項目（已使用）
- ❌ `batch2_retry.csv` - 3,923 個待重試的項目（明天用）

**分類邏輯**:
```
成功: City 有值 ✓ Country ≠ "未知" ✓
失敗: City 為空 ✗ OR Country = "未知" ✗
```

### 3. 未知位置重新分類
**時間**: 約 8 分鐘（後台執行）
**內容**: 使用 723 個座標映射來重新分類 4,646 個檔案

**處理流程**:
1. 讀取 `batch2_success.csv` → 建立座標映射表（721 個有效映射）
2. 掃描 `wiki/未知/未分類/` 的 4,646 個檔案
3. 提取每個檔案的 coordinates: [lng, lat]
4. 在映射表中查詢座標
5. 標準化國家名稱（英文 → 中文）
6. 建立新目錄 `wiki/{country}/{city}/`
7. 將檔案移動到新位置

**結果統計**:
```
✅ 已移動:     689 個檔案 (14.8%)
⚠️ 座標未找到: 3,953 個檔案 (85.0%)
❌ 失敗:       4 個檔案 (0.1%)
📊 總計:       4,646 個檔案
```

**失敗原因分析**:
- 3,953 個未找到：這些檔案的座標不在 723 個成功編碼中，需要等待第二批地理編碼
- 4 個失敗：可能是目錄創建或檔案移動過程中的臨時錯誤

## 📊 當前 Wiki 結構狀態

### 分類概況
| 狀態 | 數量 | 位置 |
|------|------|------|
| 已分類 | 約 2,000+ | `wiki/{country}/{city}/` |
| 待分類 - 未知資料夾 | 3,957 | `wiki/未知/未分類/` |
| 待分類 - 其他資料夾 | 130 | `wiki/其他/*/` |
| **總計** | **6,087+** | - |

### 剩餘檔案分布
```
wiki/
├── 未知/
│   └── 未分類/      (3,957 個檔案) ← 需要第二批地理編碼
├── 其他/
│   └── (多個城市資料夾)  (130 個檔案) ← 需要第二批地理編碼
└── (17 個國家資料夾)/   (已分類的檔案)
    └── (多個城市資料夾)/
```

## 🔄 明天的工作計畫

### Step 1: 上傳並編碼第二批
1. 上傳 `batch2_retry.csv`（3,923 個座標）到 Google Sheets
2. 執行 Google Apps Script 的 `reverseGeocode()` 函數
3. 等待編碼完成（需要 ⏱️ 約 30-60 分鐘，會受到 Google Geocode API 配額限制）

### Step 2: 自動重新分類
1. 下載編碼結果為 CSV
2. 告訴 Claude Code：檔案位置或直接調用
   ```
   /classifyUnknown
   ```
3. Claude Code 會自動：
   - 讀取新 CSV
   - 分類所有新編碼的檔案
   - 生成統計報告

### 預期結果（第二批完成後）
如果第二批編碼成功編碼 ~3,500 個座標，將有：
- ✅ 約 4,200+ 個檔案會被分類到正確的國家/城市
- ⏳ 約 500-700 個檔案可能仍失敗（需要手動處理）

## 📁 重要檔案位置

### CSV 檔案
```
h:\我的雲端硬碟\llm_wiki_travel\
├── batch2_success.csv (723 個 ✅)
├── batch2_retry.csv (3,923 個 ❌ - 明天用)
├── unknown_coordinates_batch2.csv (原始 4,646 個)
└── (其他歷史檔案)
```

### 可執行腳本
```
scripts/
└── classify_unknown_batch.ps1 (可重用的 PowerShell 腳本)
```

### Skill 文檔
```
.claude/
└── skills/
    └── classifyUnknown/
        └── SKILL.md (技能文檔)
```

## 🛠️ 技術細節

### CSV 格式標準
```
Title,Longitude,Latitude,City,Country
"location-name",lng,lat,"city-name",country-name
```

### 座標提取模式
```regex
coordinates:\s*\[([^,]+),\s*([^\]]+)\]
```

### 檔案移動邏輯
```powershell
source:      wiki/未知/未分類/{filename}.md
destination: wiki/{country}/{city}/{filename}.md
```

## 📝 已生成的 Skill

### /classifyUnknown
**用途**: 自動重新分類已編碼的檔案
**調用**: `/classifyUnknown`
**工作流程**:
1. 讀取最新的成功 CSV
2. 建立座標映射
3. 掃描 wiki/未知 和 wiki/其他
4. 重新分類所有可匹配的檔案
5. 輸出詳細統計

## ⚡ 效率統計

| 任務 | 耗時 | 檔案數 | 效率 |
|------|------|--------|------|
| 資料夾整合 | 5 分鐘 | 248 | 49.6 個/分鐘 |
| CSV 分離 | 2 分鐘 | 4,646 | 2,323 個/分鐘 |
| 重新分類 | 8 分鐘 | 4,646 | 581 個/分鐘 |
| **總計** | **15 分鐘** | **~6,000** | **~400 個/分鐘** |

## 🎓 學習與改進

### 工作中遇到的問題
1. **PowerShell Join-Path 限制**: 最多只接受 3 個參數，需要用字符串拼接或嵌套調用
2. **CSV 編碼問題**: 某些失敗的編碼結果出現亂碼，可能是 Google Sheets 的字符集問題
3. **Google Geocode API 配額**: 一天的配額有限（通常 ~2,500 次），大批量編碼需要分多天進行

### 解決方案
1. ✅ 使用字符串拼接構建路徑：`"$path\$subfolder"`
2. ✅ 在 CSV 分類時，已將失敗項分離到 `batch2_retry.csv`
3. ✅ 計畫分批上傳和編碼，避免超配額

### 未來優化方向
- 考慮使用離線地理編碼資料庫（如 GeoIP）來加速未來的批量處理
- 為 Wiki 建立一個專用的地理編碼快取
- 實現自動重試機制，對於失敗的座標嘗試多次查詢

## 📌 重要提醒

- ⚠️ 檔案移動是**破壞性操作**，一旦移動無法自動恢復
- ⚠️ 確保 CSV 資料正確無誤再執行分類
- ⚠️ 如果有異常，優先檢查座標映射是否正確
- ✅ 所有操作都已記錄，可以追蹤每個檔案的移動歷史

## 📞 下次使用指南

```
明天的工作流程：

1️⃣ 上傳 batch2_retry.csv 到 Google Sheets
2️⃣ 執行 Google Apps Script reverseGeocode()
3️⃣ 下載結果
4️⃣ 告訴我位置，或直接調用：
   /classifyUnknown
5️⃣ 完成！我會自動分類並報告進度
```

---

**文檔生成時間**: 2026-05-09
**下一階段**: 等待第二批地理編碼完成
**預計下一步**: 2026-05-10 或更晚
